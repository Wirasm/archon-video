Write one full narration script for each of the three openings below, for the short video the brief describes.

$preflight.output.context

The openings, best first according to a pairwise judge:

$rank.output.top

For each opening, write the whole narration that starts with its spoken line (you may tighten that line) and runs $preflight.output.length_min to $preflight.output.length_max seconds when read aloud at about 150 words per minute.

- Every character is read aloud by a text-to-speech voice: plain spoken English, no markdown, no emoji, no stage directions, no speaker labels. Write numbers and symbols as words ("ninety percent", not "90%").
- Short sentences, one idea each. The picture will cut where the meaning moves, so give it places to move.
- Pass the 3-second test: by the end of the first sentence a stranger scrolling past knows what they get for watching to the end.
- Raise one open question early and answer it inside the video, before the closing line. Pay off the opening's promise inside the video. Keep every claim true and specific.
- End with a line that makes the first line land again, so the loop feels deliberate.
- `overlay` is the opening's on-screen text and `first_frame` its opening shot, for this script (keep or improve the opening's).
- `title` is a short working title. `notes` says in one or two sentences what makes this version different from the other two.
- Set `hook_id` to the opening's id.
