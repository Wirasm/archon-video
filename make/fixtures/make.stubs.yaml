# Stubs for a zero-cost graph test:
#   archon workflow run Wirasm/archon-video:make --dry-run \
#     --stubs <pack>/make/fixtures/make.stubs.yaml --input topic=x
preflight:
  kind: social
  format: shorts
  config_digest: "0000000000000000"
  length_min: 25
  length_max: 45
  moods: []
  context: "## Brand: Acme"
